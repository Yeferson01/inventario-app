import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Marca y UX Principal (Brand Identity)
  static const Color primary = Color(0xFF1E293B);    // Slate Dark (Muy elegante para SaaS)
  static const Color secondary = Color(0xFF0EA5E9);  // Sky Blue (Moderno y tecnológico)
  
  // Variaciones opcionales para capas visuales específicas
  static const Color accent = Color(0xFF6366F1);     // Indigo
}