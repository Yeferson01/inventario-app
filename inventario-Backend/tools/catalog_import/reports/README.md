# Reportes locales

Use este directorio para reportes JSON de `dry-run`. Los reportes se generan
explícitamente; el tooling no escribe aquí durante `validate` ni durante sus
tests.

Antes de compartir un reporte, recuerde que puede contener nombres, códigos y
referencias de procedencia del dataset en curación.
